appVersion: v1
kind: Service
metadata:
  name: {{ .Values.front.name }}-canary
  namespace: {{ .Values.namespace.name }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.back.name }}-app
  ports:
    - port: 80
      targetPort: 80
