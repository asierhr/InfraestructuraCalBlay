apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.back.name }}
  namespace: {{ .Values.namespace.name }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.back.name }}-app
  ports:
    - port: 8080
      targetPort: 8080
