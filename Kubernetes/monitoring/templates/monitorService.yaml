apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ptin-back-monitor
  labels:
    release: monitor
spec:
  namespaceSelector:
    any: true
  selector:
    matchLabels:
      app: ptin-back-app
  endpoints:
  - port: http 
    path: /actuator/prometheus
    interval: 15s
