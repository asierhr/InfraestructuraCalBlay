apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-testing
  namespace: testing
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-testing
  resources:
    requests:
      storage: 5Gi
